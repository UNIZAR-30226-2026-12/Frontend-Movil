package com.example.random_reversi.ui.theme

import androidx.compose.ui.graphics.Color

// === Paleta estilo Random Reversi (cartoon/retro) ===

// Fondo principal: teal oscuro del mosaico
val BgColor          = Color(0xFF1B6B6B)

// Superficies: beige/crema (tarjetas estilo washi-tape)
val SurfaceColor     = Color(0xFFF5EDDA)
val SurfaceLightColor= Color(0xFFFAF3E4)

// Primario: rojo coral (botones principales, fichas rojas)
val PrimaryColor     = Color(0xFFE8593A)
val PrimaryHoverColor= Color(0xFFCF4A2E)

// Secundario: amarillo mostaza
val SecondaryColor   = Color(0xFFD4A82A)

// Acento verde (fichas verdes, washi-tape verde)
val AccentGreen      = Color(0xFF5B9B6B)

// Texto principal oscuro (sobre superficies claras)
val TextColor        = Color(0xFF2C1A0E)

// Texto secundario/muted (sobre superficies claras)
val TextMutedColor   = Color(0xFF7A5C3A)

// Borde/outline (beige oscuro, estilo tira adhesiva)
val BorderColor      = Color(0xFFB8A080)

// Color de texto sobre fondo oscuro (teal)
val TextOnDark       = Color(0xFFF5EDDA)
